ktor {
    deployment {
        port = 8081
        port = ${?PORT}
    }
    application {
        modules = [ com.codersee.ApplicationKt.module ]
    }
}
jwt {
    audience = "my-audience"
    issuer = "http://localhost/"
    realm = "My realm"
    secret = "Hello World"
}